{
  "name": "workspace-greeter",
  "tools": [
    {
      "name": "greet_workspace",
      "description": "Return a short greeting for the current workspace name",
      "kind": "template",
      "template": "Hello from skill pack — workspace hint: {name}",
      "parameters": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "Workspace or project name"}
        },
        "required": ["name"]
      }
    }
  ]
}
